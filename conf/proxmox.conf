[proxmox]
# The name or IP where to reach the Proxmox REST API. May include a port number
# separated by colon if different from the default 8006.
hostname = 192.168.1.24

# Name and password of the user to use for logging into the Proxmox REST API.
# The username might need to include a realm name appended with an @.
username = cuckoo@pve
password = 

# Default network interface.
interface = ens19

# FOG on PROXMON options
# Action done to VM
action = reset # meaning reset to the snapshot taken.
nodes = pve01  # Get the name right. don't ask me why I'm saying that :( 

# Specify a comma-separated list of available machines to be used. For each
# specified ID you have to define a dedicated section containing the details
# on the respective machine. (E.g. cuckoo1,cuckoo2,cuckoo3)
machines = Win10VM

[Win10VM]
# Specify the label name of the current machine as specified in your
# libvirt configuration.
label = Client-Cape # name of the machine on proxmox, you can see it w/ `qm list`

# Specify the operating system platform used by current machine
# [windows/darwin/linux].
platform = windows

# Specify the IP address of the current virtual machine. Make sure that the
# IP address is valid and that the host machine is able to reach it. If not,
# the analysis will fail. You may want to configure your network settings in
# /etc/libvirt/<hypervisor>/networks/
ip = 192.168.100.200

# Set the machine architecture
# Required to auto select proper machine architecture for sample
# x64 or x86
arch = x64

# (Optional) Specify tags to display
# Tags may be used to specify on which guest machines a sample should be run
# tags = windows_xp_sp3,acrobat_reader_6
tags = x64 # should not be needed but is no issue.
# (Optional) Specify the snapshot name to use. If you do not specify a snapshot
# name, the KVM MachineManager will use the current snapshot.
# Example (Snapshot1 is the snapshot name):
snapshot = cape-clean # I'm using this snapshot for now, I'll setup multiple w/ different levels of security I think.

# (Optional) Specify the name of the network interface that should be used
# when dumping network traffic from this machine with tcpdump. If specified,
# overrides the default interface specified in auxiliary.conf
# Example (virbr0 is the interface name):
interface = vmbr0

# (Optional) Specify the IP of the Result Server, as your virtual machine sees it.
# The Result Server will always bind to the address and port specified in cuckoo.conf,
# however you could set up your virtual network to use NAT/PAT, so you can specify here
# the IP address for the Result Server as your machine sees it. If you don't specify an
# address here, the machine will use the default value from cuckoo.conf.
# NOTE: if you set this option you have to set result server IP to 0.0.0.0 in cuckoo.conf.
# Example:
resultserver_ip = 192.168.100.100 # cape server ip

# (Optional) Specify the port for the Result Server, as your virtual machine sees it.
# The Result Server will always bind to the address and port specified in cuckoo.conf,
# however you could set up your virtual network to use NAT/PAT, so you can specify here
# the port for the Result Server as your machine sees it. If you don't specify a port
# here, the machine will use the default value from cuckoo.conf.
# Example:
# resultserver_port = 2042

# When FOG is on PROXMON
[Node_1]
# machineName(on the FOG server) = machineID(on Proxmox)
# i.e.:
# win7_1 = 120
# win7_2 = 121
# win10_1 = 126
Win10VM = 104 # [VM_NAME] ID on Proxmox.
